# =============================================================
# INTELLMADE STUDIO - Backend Environment Variables
# =============================================================
# Copy this file and rename it to ".env" then fill in your values.
# DO NOT share your .env file with anyone or upload it to GitHub.
# =============================================================

# ----- SERVER SETTINGS -----
PORT=4000
NODE_ENV=development

# ----- SUPABASE -----
# Go to https://supabase.com → Your Project → Settings → API
# Copy "Project URL" and paste below:
SUPABASE_URL=https://your-project-id.supabase.co

# Copy "anon public" key and paste below:
SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key-here

# Copy "service_role secret" key and paste below (keep this SECRET):
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...your-service-role-key-here

# Go to Settings → API → JWT Settings → Copy "JWT Secret":
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ----- STRIPE -----
# Go to https://dashboard.stripe.com → Developers → API keys
# Copy "Secret key" (starts with sk_test_ or sk_live_):
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here

# Go to Developers → Webhooks → Add endpoint → Copy "Signing secret":
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-signing-secret-here

# These are the Price IDs from Stripe for your subscription plans.
# Go to Products → Create product → Copy the Price ID (starts with price_):
STRIPE_PRICE_BASIC=price_your-basic-plan-price-id
STRIPE_PRICE_PRO=price_your-pro-plan-price-id
STRIPE_PRICE_ENTERPRISE=price_your-enterprise-plan-price-id

# ----- GOOGLE GEMINI AI -----
# Go to https://aistudio.google.com/apikey → Create API key:
GEMINI_API_KEY=your-gemini-api-key-here

# ----- FRONTEND URL -----
# The URL where your React frontend runs (used for CORS and redirects):
FRONTEND_URL=http://localhost:3000
